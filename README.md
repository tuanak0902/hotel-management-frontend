# hotel-management-frontend
hotel managment frontend with React + Tailwind CSS

## Run locally with Docker
docker build -t hotel-management-frontend:prod .
docker run --name hotel-frontend -p 3000:80 hotel-management-frontend:prod

